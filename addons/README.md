# Future Home Assistant add-ons

This directory is reserved for future Home Assistant OS add-ons maintained in
this repository. Each add-on must have its own directory containing at least a
`config.yaml` and the files required to build and run it.
